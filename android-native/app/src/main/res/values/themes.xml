<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash + bridge to the Compose theme.
         The real theming happens inside Compose (TBirdTheme) so colors
         can be driven by Material You dynamic color on Android 12+. -->
    <style name="Theme.TBird.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/seed</item>
        <item name="postSplashScreenTheme">@style/Theme.TBird</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.TBird" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
